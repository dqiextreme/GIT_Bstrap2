<div class="row"><div class="col-lg-10 col-lg-offset-1 col-md-10 col-md-offset-1 col-sm-10 col-sm-offset-1">
    <table>
    @foreach (var item in ViewBag.res2)
    {
        <tr>
            <td>@item.ID</td>
            <td>@item.Archivo</td>
            <td>@item.Ruta</td>
        </tr>
    }
    </table>
</div></div>